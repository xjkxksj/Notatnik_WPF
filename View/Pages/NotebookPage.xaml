<Page x:Class="Notatnik_WPF.NotebookPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Notatnik_WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NotebookPage">

    <Grid Background="#d7d9ce">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="15*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="6*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Text="Notebook" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Column="0" Grid.Row="1">
            <Button Content="Add Note" Style="{DynamicResource RoundButton}" Command="{Binding AddNoteCommand}" />
            <Expander Name="myExpander"
                  Header="Filters" 
                  ExpandDirection="Down" IsExpanded="False">

                <local:SearchFilters />
            </Expander>
        </StackPanel>

        
        <local:SearchBar Grid.Column="1" Grid.Row="0" Margin="10,5,10,10"/>


        <Border Grid.Column="1" Grid.Row="1"
        CornerRadius="10,10,0,0" BorderBrush="Black" Background="Snow">

            <StackPanel Grid.Column="1" Grid.Row="1">
                <TextBlock Text="Notatnik" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>


                <ScrollViewer>
    
                    <ItemsControl ItemsSource="{Binding Repository.Notes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <local:NoteItem/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </ScrollViewer>
                
            </StackPanel>
        </Border>


       
    </Grid>
</Page>
